---
title: Submit Button
description: Submit button is a specialized reusable button component for shaddy form.
---

`SubmitButton` is a specialized, reusable button component for the Shaddy Form. it accepts all the generic button props. It also accepts a loading state and custom labels. This button should be used within shaddy form to submit the form data.


### Installation

```bash
npx shadcn add https://shaddy-docs.vercel.app/r/submit-button
```


### Usage

```tsx
<ShaddyForm>
  <div className="space-y-4">
    <TextField<Type>
      name="email"
      label="Email"
      required
      placeholder="Enter your email"
    />

    <SubmitButton label="Submit" />
  </div>
</ShaddyForm>
```