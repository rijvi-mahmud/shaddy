---
title: useBoolean Hook
description: A custom React hook for managing boolean state values in a convenient way.
---



`useBoolean` is a custom React hook that provides utilities for managing boolean state values in a more convenient way than the standard `useState`.

It allows you to easily toggle, set, and reset boolean values, making it particularly useful for UI components that require simple on/off states, such as toggles, switches, and modals.

## useBoolean Hook
<ComponentPreview name="use-boolean"/>

### Installation

The hook is included with the template. You can import it directly in your components:

```tsx
import { useBoolean } from "@/hooks/use-boolean";
```

### Usage

```tsx
const [value, { toggle, setTrue, setFalse, reset, setValue, getValue }] = useBoolean(false);
```

### Parameters

- `initialValue` (boolean): The initial state value.

### Return Value

The hook returns an array with two elements:

1. `value` (boolean): The current state value.
2. An object containing the following methods:
   - `setValue(value: boolean)`: Sets the state to a specific boolean value.
   - `toggle()`: Toggles the state between true and false.
   - `setTrue()`: Sets the state to true.
   - `setFalse()`: Sets the state to false.
   - `reset()`: Resets the state to its initial value.
   - `getValue()`: Returns the current state value.

### Examples

#### Basic Usage

```tsx
import { useBoolean } from "@/hooks/use-boolean";

function ToggleComponent() {
  const [isVisible, { toggle }] = useBoolean(false);
  
  return (
    <div>
      <button onClick={toggle}>
        {isVisible ? 'Hide Content' : 'Show Content'}
      </button>
      
      {isVisible && <div>This content can be toggled</div>}
    </div>
  );
}
```

#### Using Multiple Control Functions

```tsx
import { useBoolean } from "@/hooks/use-boolean";

function ExpandableSection() {
  const [isExpanded, { setTrue, setFalse }] = useBoolean(false);
  
  return (
    <div>
      <div onMouseEnter={setTrue} onMouseLeave={setFalse}>
        Hover over me
      </div>
      
      {isExpanded && (
        <div>
          Additional information that appears on hover
        </div>
      )}
    </div>
  );
}
```

#### Form Submission Example

```tsx
import { useBoolean } from "@/hooks/use-boolean";

function SubmitForm() {
  const [isSubmitting, { setTrue, setFalse }] = useBoolean(false);
  
  const handleSubmit = async (e) => {
    e.preventDefault();
    setTrue(); // Set loading state to true
    
    try {
      // Simulate API call
      await new Promise(resolve => setTimeout(resolve, 1000));
      console.log('Form submitted successfully!');
    } catch (error) {
      console.error('Error submitting form:', error);
    } finally {
      setFalse(); // Set loading state back to false
    }
  };
  
  return (
    <form onSubmit={handleSubmit}>
      {/* Form fields */}
      <button type="submit" disabled={isSubmitting}>
        {isSubmitting ? 'Submitting...' : 'Submit'}
      </button>
    </form>
  );
}
```

## Features

- **Beautiful and responsive design**: The template is responsive and adapts to any screen size.
- **Ready-to-use components**: Use the ready-to-use components to create a beautiful and informative documentation.
- **Customizable**: Customize the template to fit your project.
- **Easy to use**: The template is easy to use and you can start documenting your project quickly.
- **Internationalization**: The template supports multiple languages.
- **Internationalized sitemap**: The sitemap is internationalized and you can add links to different languages.
- **SEO optimized**: The template is optimized for SEO.
- **Ready for production**: The template is ready for production and you can deploy it easily.
- **Dark and light theme**: The template supports dark and light themes.
- **Accessible**: The template aims to be accessible to everyone.
- **Open Source**: The template is open source and you can contribute to it on GitHub.

those are some of the features of the template!

